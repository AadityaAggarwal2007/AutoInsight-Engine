# AutoInsight-Engine
AutoInsight-Engine is a schema-agnostic Python analytics engine that converts raw CSV/Excel data into business KPIs, trends, Pareto insights, executive summaries, and guided next steps — automatically, without hardcoded columns or manual configuration.
